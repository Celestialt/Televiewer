<div class="booyah-box col-10 offset-1">

	<h1>Add New T.V. Show to Watch List:</h1>

	<%= simple_form_for @show do |f| %>
		<%= f.input :title %>
		<%= f.input :genre %>
		<%= f.input :year %>
		<%= f.submit 'Post!', class: 'my-btn' %>
	<% end %>
</div>